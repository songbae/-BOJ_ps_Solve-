#include<bits/stdc++.h>
#define f ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
#include <regex>
#define MAX 1000000009
using namespace std;
string s;
string T;
int main() {
	cin >> s >> T;
	regex cmp1(s);
	string s1 = reverse(s.begin(),s.end());
	regex cmp2();
	int size = s.size();
	T = regex_replace(T, cmp1, "");
		if()
	}
	
}